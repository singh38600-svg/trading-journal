rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only the backend service account can read/write — block all browser access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
